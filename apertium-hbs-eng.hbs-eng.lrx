<rules>
  <rule>
       <or>
	    <match lemma="lijepo" tags="*"/>
	    <match lemma="ružno" tags="*"/>
       </or>
	  <match lemma="vrijeme" tags="n.*">
	    <select lemma="weather" tags="n.*"/>
	  </match>
	</rule>

</rules>
