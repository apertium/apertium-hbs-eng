<rules>
  <rule>
       <or>
	    <match lemma="lijepo" tags="*"/>
	    <match lemma="ružno" tags="*"/>
       </or>
	  <match lemma="vrijeme" tags="n.*">
	    <select lemma="weather" tags="n.*"/>
	  </match>
  </rule>

  <rule>
	  <match lemma="grad" tags="n.*"/>
	  <match lemma="Rijeka" tags="np.*">
	    <select lemma="Rijeka" tags="np.*"/>
	  </match>
  </rule>

</rules>
